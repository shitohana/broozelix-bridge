# broozelix-bridge

Thin Helix socket bridge for [broozelix](https://github.com/shitohana/broozelix). Subscribes to Helix events and runs configured handlers; also provides `open` and `remote` helpers used by Broot.

Not intended for standalone use — install via broozelix or `nix build`.
